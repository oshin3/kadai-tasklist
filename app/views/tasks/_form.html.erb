<%= form_with(model: tasks) do |f| %>
  <%= render 'layouts/error_messages', model: f.object %>

  <%= f.label :tasks, 'ステータス' %>
  <%= f.text_field :status %>

  <%= f.label :tasks, 'タスク' %>
  <%= f.text_field :content %>

  <%= f.submit '追加' %>
<% end %>